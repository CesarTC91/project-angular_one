const username: string | number = 'polaco91';

const sum = (num1: number, num2: number) => {
  return num1 + num2;
}
sum(25, 3)

class Person{

  constructor(public age: number, public lastname: string){}
}

const polaco = new Person(32, 'Tobon');

